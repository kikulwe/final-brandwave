class CustomUser {

  final String uid;
  final String ? email;
  final String ? phoneNumber;
  final String ? photoURL;
  // final String lastname;

  CustomUser({
    required this.uid,
    this.email,
    this.phoneNumber,
    this.photoURL,

  });

}